pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo $GERRIT_REFSPEC
                source share/spack/setup-env.sh
                echo $(which spack)
            }
        }
    }

}
