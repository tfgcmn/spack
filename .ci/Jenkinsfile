#!/usr/bin/env groovy

node {
    stage('Build') {
        sh "echo ${GERRIT_REFSPEC}"
        sh "echo ${PWD}"
        dir('spack')
        sh "source share/spack/setup-env.sh"
        sh "spack list"
    }
}
