#!/usr/bin/env groovy

node {
    stage('Build') {
        sh "echo ${GERRIT_REFSPEC}"
        sh "echo ${PWD}"
        sh "echo \$5(ls)"
        sh ". share/spack/setup-env.sh"

        sh "echo \$5(which spack)"
    }
}
